import { Environment } from 'src/app/models/environment-model';

export const environment: Environment = {
  production: true,
  envName: "prod",
  apiBasePath: "https://",
  apiAdminBasePath: "https://"
};